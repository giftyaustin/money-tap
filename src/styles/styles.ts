import { CSSProperties } from "react";

export const errorToastStyles:CSSProperties = {
    backgroundColor: "red",
    color: "white",
    fontWeight: "bold",
    padding: "10px",
    borderRadius: "5px",
}